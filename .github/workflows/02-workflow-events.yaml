name: 02-workflow-events.yaml

on:
    pull_request:  
    workflow_dispatch:
    
jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show the trigger
              run: echo "workflow was triggered by ${{github.event_name}} event."